[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --color=yes
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    error_handling: Error handling tests
    fixtures: Tests that use fixtures